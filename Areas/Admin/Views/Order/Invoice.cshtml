<!-- WebsiteBanHang/Areas/Admin/Views/Order/Invoice.cshtml -->
@model Web_dienmay.Models.Order

@{
    ViewData["Title"] = "Hóa đơn #" + Model.Id;
    var invoiceHtml = ViewBag.InvoiceHtml;
}

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Hóa đơn #@Model.Id</h2>
        <div>
            <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left me-2"></i>Quay lại
            </a>
            <a asp-action="DownloadInvoice" asp-route-id="@Model.Id" class="btn btn-primary">
                <i class="bi bi-download me-2"></i>Tải về PDF
            </a>
            <button onclick="printInvoice()" class="btn btn-success">
                <i class="bi bi-printer me-2"></i>In hóa đơn
            </button>
        </div>
    </div>

    <div class="card shadow-sm">
        <div class="card-body p-0">
            <div id="invoice-content">
                @Html.Raw(invoiceHtml)
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function printInvoice() {
            const printContent = document.getElementById('invoice-content').innerHTML;
            const originalContent = document.body.innerHTML;

            document.body.innerHTML = printContent;
            window.print();
            document.body.innerHTML = originalContent;

            // Reload scripts after printing
            location.reload();
        }
    </script>
}
